import { ReceitasClient } from "./ReceitasClient";

export default function ReceitasPage() {
  return (
    <main className="gap-lg p-lg mx-auto flex w-full max-w-7xl flex-col">
      <ReceitasClient />
    </main>
  );
}
